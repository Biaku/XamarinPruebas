<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Pruebas.ViewModels;assembly=Pruebas"
             x:Class="Pruebas.Views.HttpClientDemo">
    <ContentPage.Content>
        <StackLayout BindingContext="{vm:HttpCliendDemoViewModel}">
            <Label Text="Ejemplo de HttpClient" />
            <ListView
                ItemsSource="{Binding Publicaciones}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>